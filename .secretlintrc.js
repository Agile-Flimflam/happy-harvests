module.exports = {
  rules: [
    {
      id: '@secretlint/secretlint-rule-preset-recommend',
    },
  ],
  // Allow patterns that are known to be safe (e.g., example/test values)
  allowMessageIds: [],
};
